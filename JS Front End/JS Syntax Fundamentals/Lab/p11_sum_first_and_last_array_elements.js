function solve(arr) {
    let firstNumber = arr[0]
    let lastNumber = arr[arr.length - 1]
    let result = firstNumber + lastNumber
    console.log(result)
}

solve([20, 30, 40])
solve([10, 17, 22, 33])
solve([11, 58, 69])